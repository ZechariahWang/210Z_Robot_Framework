#include "main.h"

void SoloWinPoint(){
    // Stuff will be added when actual robot is made
}


void testWP(){
	// PID_eclipse.set_pid_targets(1, 0, 1.2, 1.2);
	// PID_eclipse.combined_TranslationPID(-100, 300, -200, true, false);

	// PID_eclipse.set_pid_targets(1, 0, 1.2, 1.2);
	// PID_eclipse.combined_TranslationPID(100, 300, -200, true, false);

	// PID_eclipse.set_turn_pid_targets(2.3, 0.002, 0);
	// PID_eclipse.combined_TurnPID(48);

	// PID_eclipse.set_pid_targets(1, 0, 1.2, 1.2);
	// PID_eclipse.combined_TranslationPID(4600, 300, -200, true, false);

	// PID_eclipse.set_turn_pid_targets(2.3, 0.002, 0);
	// PID_eclipse.combined_TurnPID(270);

	// PID_eclipse.set_pid_targets(1, 0, 1.2, 1.2);
	// PID_eclipse.combined_TranslationPID(-100, 400, -200, true, false);
}